/* Archived backup file. The contents were moved to src/archive/MapCardModern.module.css.backup */

/* If you need to restore the backup, copy from src/archive/MapCardModern.module.css.backup */

    min-height: 180px;
  }
}

@media (max-width: 480px) {
  .mapCardModern {
    position: fixed;
    top: var(--app-header-height, 64px); /* pick from JS */
    left: 0;
    width: 100vw;
    height: calc(100vh - var(--app-header-height, 64px)); /* Da ne ide ispod headera */
    z-index: 10;
    border-radius: 0;
    margin: 0;
    padding: 0;
  }
  .addressBarIntegrated { padding: 8px; z-index: 2000; }
  .mapFooter {
    padding: 8px;
  }
  .searchInput {
    font-size: 16px;
  }
  .suggestionsDropdown {
    left: 0;
    right: 0;
    width: 100%;
    box-sizing: border-box;
  }
  .currentAddress, .selectedAddress {
    font-size: 12px;
  }
}